# User Preferences

User Preferences store small UI preference payloads for a user.

## Examples

- Theme preference
- List filter state
- List sort state
- Map viewport and basemap preferences

Preferences are keyed JSON values scoped to one user. A user can have one value
per preference key.

Preferences are application-owned UI state, not canonical planning records.
